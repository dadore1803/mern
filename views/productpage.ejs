<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= data.name %> | Myntra Style</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet" />

  <style>
    body {
      font-family: 'Segoe UI', sans-serif;
      background-color: #f9f9f9;
    }

    .product-container {
      display: flex;
      flex-direction: column;
      gap: 30px;
      margin-top: 40px;
    }

    @media (min-width: 768px) {
      .product-container {
        flex-direction: row;
      }
    }

    .image-gallery img {
      width: 100%;
      border-radius: 10px;
      margin-bottom: 15px;
    }

    .product-info h3 {
      font-size: 1.8rem;
      font-weight: 600;
    }

    .product-info .price {
      color: #e53935;
      font-size: 1.4rem;
      margin: 15px 0;
    }

    .product-info .btn {
      margin-right: 10px;
      border-radius: 30px;
      padding: 8px 20px;
    }

    .product-description {
      color: #555;
      margin-top: 20px;
    }
  </style>
</head>
<body>
<%- include('assets/header.ejs')%>

<div class="container product-container">

  <div class="col-md-6 image-gallery">
    <% if (data.images && data.images.length > 0) { %>
      <% data.images.forEach((img) => { %>
        <img src="<%= img %>" alt="Product Image">
      <% }) %>
    <% } else { %>
      <img src="/images/no-image.png" alt="No image available">
    <% } %>
  </div>

 
  <div class="col-md-6 product-info">
    <h3><%= data.name %></h3>
    <p class="text-muted">Brand: <%= data.brand %></p>
    <div class="price">₹<%= data.price %></div>

    <div class="d-flex align-items-center mb-3">
      <a href="/cart/<%= data._id %>"><button class="btn btn-dark">Add to Bag</button></a>
      <button class="btn btn-outline-danger"><i class="bi bi-heart"></i> Wishlist</button>
    </div>

    <div class="product-description">
      <h5>Product Description</h5>
      <p><%= data.description %></p>

      <p><strong>Category:</strong> <%= data.category %></p>
    </div>
  </div>
</div>


<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
